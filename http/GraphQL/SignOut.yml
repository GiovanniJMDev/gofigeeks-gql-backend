info:
  name: SignOut
  type: graphql
  seq: 6

graphql:
  method: POST
  url: "{{host}}"
  body:
    query: |
      mutation SignOut {
        SignOut
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
